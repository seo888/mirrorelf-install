# 复制为 env.hub 后按需修改：
#   cp env.hub.example env.hub
#
MIRRORELF_IMAGE=seo888/mirrorelf:latest
# 宿主机 5432 已被占用（如 1Panel PostgreSQL）时改为 15432
MIRRORELF_PG_HOST_PORT=5432
# 须与 compose 中 POSTGRES_PASSWORD 一致
MIRRORELF_PG_PASSWORD=mirrorelf
#
# 管理后台：http://<主机>:16888/_/admin/
